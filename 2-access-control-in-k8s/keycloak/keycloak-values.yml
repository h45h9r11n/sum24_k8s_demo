production: true
auth:
  createAdminUser: true
  adminUser: admin
  adminPassword: admin
tls:
  enabled: true
  autoGenerated: true
ingress:
  enabled: true
  hostname: keycloak.keycloak-auth.lab
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTPS
  servicePort: https
  tls: true
  extraTls:
  - hosts:
    - keycloak.keycloak-auth.lab
    secretName: keycloak-tls
postgresql:
  enabled: true
  postgresqlPassword: psqldbpass12345
